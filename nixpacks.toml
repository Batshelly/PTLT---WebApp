[phases.setup]
nixPkgs = ["python311", "gcc", "mysql80", "pkg-config", "cairo", "pango", "gdk-pixbuf", "libxml2", "libxslt"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build phase complete'"]

[start]
cmd = "gunicorn PTLT.wsgi --bind 0.0.0.0:$PORT --log-file -"
